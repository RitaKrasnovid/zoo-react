pipeline {
  agent any
  // triggers {
  //   pollSCM('*/5 * * * *')
  // }
  stages{
      stage ('Init'){
        steps {
          echo 'init...'
        }
       }
       stage ('Build') {
         steps {
          echo 'Build'
        }
       }
       stage ('Deploy') {
         steps {
          echo 'Deploy stop ...'
        }
       }
    }
}
